# Zypher AI Logo Generator - Requirements

# Web Framework (NEW ChatGPT-style interface)
flask>=3.0.0
flask-cors>=4.0.0

# Environment Variables
python-dotenv>=1.0.0

# Core Dependencies (Original Gradio interface)
gradio>=4.0.0
torch>=2.0.0
diffusers>=0.25.0
transformers>=4.35.0
accelerate>=0.25.0
safetensors>=0.4.0

# Image Processing
pillow>=10.0.0

# Additional Utilities
numpy>=1.24.0
huggingface-hub>=0.19.0

# Optional: For better performance
# xformers>=0.0.22  # Uncomment for faster attention (requires compatible GPU)
